services:
  jupyter:
    image: jupyter/scipy-notebook:latest
    container_name: mlops-lab-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ../../:/home/jovyan/work
    environment:
      JUPYTER_ENABLE_LAB: "yes"
    working_dir: /home/jovyan/work
    restart: unless-stopped
